import React from "react";
import MobileResetPasswordScreen from "../../mobile/auth/ResetPasswordScreen";

export default function ResetPasswordScreen(props: any) {
  return <MobileResetPasswordScreen {...props} />;
}
